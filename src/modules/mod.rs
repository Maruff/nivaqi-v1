// src/modules/mod.rs

pub mod base;  // Declare the `base` module inside `modules`
pub mod accounting;  // Declare the `accounting` module inside `modules`
pub mod inventory;  // Declare the `inventory` module inside `modules`
pub mod sales;  // Declare the `sales` module inside `modules`
pub mod purchase;  // Declare the `purchase` module inside `modules`
pub mod invoice;  // Declare the `invoice` module inside `modules`